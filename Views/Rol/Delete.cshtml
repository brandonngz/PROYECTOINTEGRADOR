@model ProyectoIntegrador.Models.Rol
@{
    ViewData["Title"] = "Eliminar";
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="~/css/delete.css">
    <title>Document</title>
</head>
<body> @**@
        <div class="containers">
            <header class="header">
                    <h4>ELIMINACIÓN DE ROL <i class="bi bi-gear-fill"></i></h4>   
            </header>  
                <div class="main">
                     <div class="editt">
                        <h5>Rol ↴</h5>
                        <dl class="row"> @*Lista de descripcion*@
                            <dt>@*Texto de descripcion, titulo*@
                                @Html.DisplayNameFor(modelo => modelo.Descripcion)
                            </dt>
                            <dd>@*element contains data that describes a definition term*@
                                @Html.DisplayFor(modelo => modelo.Descripcion)
                            </dd>
                        </dl>
                        <hr>
                        <form asp-action="Delete">
                            <input type="hidden" asp-for="IdRol"/>
                            <input type="submit" onclick="Alert()" value="Eliminar" class="btn btn-danger " style="margin: 0 20px 0px 0px;"/>
                            <a asp-action="Index"class="btn btn-secondary" ><i class="bi bi-backspace"></i> Volver</a>
                        </form>
                    </div>
                </div>
        </div>

           
</body>
</html>