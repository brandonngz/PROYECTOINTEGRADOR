@model IEnumerable<ProyectoIntegrador.Models.Usuario>
@{
    ViewData["Title"] = "Menú";
}
<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<h4>Usuarios 🡣</h4>
<br>
<table class="table table-striped table-hover table-bordered ">
    <thead class="table-dark">
        <tr>
            <th scope="col">
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th scope="col">
                @Html.DisplayNameFor(model => model.Apellido)
            </th>
            <th scope="col">
                @Html.DisplayNameFor(model => model.Direccion)
            </th>
            <th scope="col">
                @Html.DisplayNameFor(model => model.Telefono)
            </th>
            <th scope="col">
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th class="text-center">
                <a asp-action="Create" class="btn btn-light  "><i class="bi bi-person-plus-fill bi-lg"></i> Nuevo Usuario</a>
            </th>
        </tr>
    </thead>
    <tbody>
    @foreach (var item in Model) {
        <tr>
            <td scope="row">
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td scope="row">
                @Html.DisplayFor(modelItem => item.Apellido)
            </td>
            <td scope="row">
                @Html.DisplayFor(modelItem => item.Direccion)
            </td>
            <td scope="row">
                @Html.DisplayFor(modelItem => item.Telefono)
            </td >
            <td scope="row" > 
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td class="text-center">
                <a asp-action="Edit" asp-route-id="@item.IdUsuario"><i class="bi bi-pen-fill " ></i></a>
                <a asp-action="Delete" asp-route-id="@item.IdUsuario" class="text-danger"><i class="bi bi-trash-fill ml-3"> </i></a>
                <a asp-action="Details" asp-route-id="@item.IdUsuario"><i class="bi bi-exclamation-square-fill ml-3"></i></a>
            </td>
        </tr>
    }
    </tbody>
</table>
</body>
</html>