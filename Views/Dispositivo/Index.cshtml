@model IEnumerable<ProyectoIntegrador.Models.Dispositivo>
@{
    ViewData["Title"] = "Menú";
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dispositivos</title>
</head>
    <body>
@**@<h4>Dispositivos 🡣</h4>
<br>
       <p><a asp-action="Create">Nuevo Dispositivo 🡸</a></p>
        <table class="table table-striped table-hover">
            <thead>
                    <th>
                        @*Enviamos como parametro al metodo, el campo de la tabla Dispositivo
                              🡻                             🡻
                                                   🡻recibe la funcion*@
                        @Html.DisplayNameFor(model => model.Nombre)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Codigo)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Ubicacion)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Descripcion)
                    </th>
                    <th></th>
                </tr>
                

            </thead>
            <tbody>
                @*Manera de comentar en una pagina CSHTML
                                        🡻Obtemos la identidad Dispositivo mediante @model         *@
                @foreach (var item in Model) //recorrer para mostrar los registros
                {
                   <tr>
                    <td>
                        @*Obtenemos el dato del foreach que estamos recorriendo y se lo asignamos a modelItem
                        Enviamos como parametro al metodo, el campo de la tabla Dispositivo
                              🡻 muestra el dato dentro de la tabla Dispositivo en el campo nombre*@      
                        @Html.DisplayFor(modelItem => item.Nombre)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Codigo)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Ubicacion)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Descripcion)
                    </td>
                    <td> @*                                 🡻Accediento al modelo, el objeto de iteraccion y accedemos a la propiedad IdEspecialidad *@
                        <a asp-action="Edit" asp-route-id="@item.IdDispositivo">Editar</a> |
                        <a asp-action="Delete" asp-route-id="@item.IdDispositivo">Eliminar</a>
                    </td>
                </tr>  
                }
            </tbody>
        </table>
        
    </body>
</html>